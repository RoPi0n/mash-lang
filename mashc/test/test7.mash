import Print   "crt.lib" "PRINT"
import PrintLn "crt.lib" "PRINTLN"
import InputLn "crt.lib" "INPUTLN"

async a:
  for i in 1..10:
    Sleep(100 + RandomTo(100))
    println("First: " + i)
  end
end

async b:
  for i in 1..10:
    Sleep(100 + RandomTo(100))
    println("Second: " + i)
  end
end

wait a
wait b

println("The end")
