uses math.plots
uses crt

func parabola(x):
  if x > 1: return null end
  return x * x
end

proc main():
  p ?= new MPlot(MPlot_Decart, MPlot_DefScale)
  p -> Draw(Parabola)
  inputln()
end
