uses <bf>
uses <crt>
uses <classes>

proc main():
  while true:
    t ?= new Tree(0)
    x ?= t -> AddNode("Hello")
    y ?= x -> AddNode("world!")
    t -> Clear()
    t -> Free()
    gc()
  end

  inputln()
end
