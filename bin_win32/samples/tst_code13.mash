uses <bf>
uses <crt>
uses <classes>

proc main():
 // while true:
    t ?= new Tree(0)

    t -> AddNode("Hello")
    t -> AddNode("world!")

    t -> Free()
    gc()
 // end

  inputln()
end
