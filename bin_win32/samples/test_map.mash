uses <bf>
uses <crt>
uses <classes>

proc main():
  while true:
  m ?= new Map()

  m -> Add("world!", 2)
  m -> Add("Hell", 0)
  m -> Add("o ", 1)

  for (i ?= 0; i < m -> Size(); i++):
    Print(m -> Get(i))
  end

  m -> Free()

  println()

  gc()
  end
  InputLn()
end
